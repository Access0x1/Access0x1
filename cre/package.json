{
  "name": "access0x1-cre-notify",
  "version": "0.1.0",
  "private": true,
  "description": "Access0x1 'Notified Settlement' — a Chainlink CRE workflow: EVM-log trigger on the router's PaymentReceived -> HTTP merchant notify + on-chain audit write to Access0x1Receiver via the KeystoneForwarder. BUILD + SIMULATE artifact (deploy is Early-Access).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "cre workflow build",
    "simulate": "cre workflow simulate",
    "simulate:broadcast": "cre workflow simulate --broadcast",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.3.1",
    "viem": "^2.34.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
