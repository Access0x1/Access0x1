{
  "name": "@access0x1/react",
  "version": "0.1.0",
  "private": true,
  "description": "Access0x1 React SDK. ENS barrel re-exports the resolution + name-math layer from the web SDK (single source of truth).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": ">=2.35.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
