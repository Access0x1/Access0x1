{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "description": "A non-custodial, USD-priced (Chainlink) crypto checkout for {{CHAIN_NAME}} — Access0x1 starter. Run `npm run setup` once to bootstrap the toolchain, then `cd app && npm run dev`.",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "contracts:build": "forge build --root contracts",
    "contracts:test": "forge test --root contracts"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
