import { FeatureGrid } from '@access0x1/web'

// Pure presentational, zero props — the marketing landing page's capability
// grid (the seven-contract product surface).
export const Default = () => <FeatureGrid />
