import { IntegrationStrip } from '@access0x1/web'

// Pure presentational, zero props — the marketing landing page's "powered by,
// and live on" trust strip (Chainlink + the settlement chains).
export const Default = () => <IntegrationStrip />
