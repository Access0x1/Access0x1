{
  "name": "@access0x1/web",
  "version": "0.1.0",
  "private": true,
  "description": "Access0x1 web SDK — ENS resolution + name-math (brand color + identicon) for the checkout/onboarding flow. Money-path safe: coinType always derived per settlement chain.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude '**/*.integration.test.ts'",
    "test:integration": "vitest run **/*.integration.test.ts",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "viem": ">=2.35.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
