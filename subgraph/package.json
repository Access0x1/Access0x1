{
  "name": "access0x1-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "Open subgraph indexing the public Access0x1Router PaymentReceived event (read-only, off the money path).",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth",
    "deploy": "graph deploy access0x1 --node https://api.studio.thegraph.com/deploy/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
